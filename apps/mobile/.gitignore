# Build nativo (regenerável)
android/app/build/
android/build/
android/.gradle/
android/captures/
android/local.properties
android/app/release/

# Keystore — NUNCA versionar
*.keystore
*.jks
keystore.properties

# Config do Firebase (fonte em secrets/google-services.json; copiar pra
# android/app/ pra buildar). Mantido fora do Git.
android/app/google-services.json

# Assets copiados pelo cap sync (vêm do apps/web/dist)
android/app/src/main/assets/public/

# Ferramentas/artefatos de release (regeneráveis, não versionar)
android/bundletool.jar
android/*.apks
android/*.apk
android/app-release-apks.zip
android/apks_out/
